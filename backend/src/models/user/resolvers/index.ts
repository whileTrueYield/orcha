/**
 * Side-effect barrel for User resolvers.
 *
 * Importing this module registers all User query and mutation fields
 * on the Pothos builder. No exports — purely side-effect imports.
 */

import "./user.resolver";
import "./users.resolver";
import "./updateUser.resolver";
import "./updateUserPreferences.resolver";
