# Core API framework
fastapi>=0.115,<1
uvicorn>=0.32,<1
gunicorn>=23,<24
pydantic>=2.10,<3

# HTTP
requests>=2.32,<3

# Data / science
numpy>=2.1,<3
pandas>=2.2,<3
scipy>=1.14,<2
matplotlib>=3.9,<4
seaborn>=0.13,<1
Pillow>=11,<12

# Progress bars (used by MCTS scheduler)
tqdm>=4.67,<5

# Dev / testing
pytest>=8,<9
# Required by fastapi.testclient.TestClient. Starlette >=1.2 prefers `httpx2`
# (the httpx successor) and emits a StarletteDeprecationWarning if only the
# legacy `httpx` is present, so we install httpx2 directly.
httpx2>=2,<3

# Profiling
snakeviz>=2.2,<3
