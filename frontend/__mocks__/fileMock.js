// Jest stub for static asset imports (images, fonts, media) — the asset path
// is irrelevant to behavior tests. Referenced by the moduleNameMapper in
// package.json's jest config.
module.exports = "test-file-stub";
